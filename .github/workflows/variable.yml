name:  Exploring Variables
on: 
  schedule:
    - cron: "*/1 * * * *"
jobs:
    docker: 
        runs-on: ubuntu:latest
        steps:
        - name: Docker Build
          run: echo docker build