name:  Exploring Variables
on: 
    workflow_dispatch: 
#   schedule:
#     - cron: "*/10 * * * *"
jobs:
    docker: 
        runs-on: ubuntu-latest
        steps:
        - name: Docker Build
          run: echo docker build

    deploy: 
        concurrency: 
            group: production
            cancel-in-progress: true
        runs-on: ubuntu:latest
        steps:
        - name: Docker Build
          run: |
           echo docker build
           sleep 200s

